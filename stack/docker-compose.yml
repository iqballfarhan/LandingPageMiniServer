version: "3.8"

services:
  landingpage:
    image: nginx:alpine
    container_name: landingpage
    restart: unless-stopped
    ports:
      - "100:80"
    volumes:
      - ./html:/usr/share/nginx/html:ro
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
